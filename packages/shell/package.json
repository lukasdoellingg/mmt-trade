{
  "name": "@mmt-trade/shell",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal HTML/TS bootloader that loads odin.js + terminal.wasm under COOP/COEP",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp -f public/terminal.wasm public/terminal.js public/terminal_smoke.wasm public/terminal_smoke.js dist/ 2>/dev/null || true",
    "preview": "vite preview"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^5.4.0"
  }
}
