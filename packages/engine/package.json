{
  "name": "@mmt-trade/engine",
  "version": "0.1.0",
  "private": true,
  "description": "Odin/Emscripten terminal.wasm — Sokol gfx + ImGui + WS-in-WASM + CBOR + layer parity with MMT.gg",
  "type": "module",
  "scripts": {
    "build": "bash build.sh",
    "build:smoke": "bash build.sh --smoke",
    "clean": "rm -rf build out"
  },
  "files": [
    "src",
    "vendor",
    "build.sh",
    "README.md"
  ]
}
