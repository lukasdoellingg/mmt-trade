#!/usr/bin/env node
/** @deprecated MMT CBOR decoder removed — first-party /ws/heatmap only. */
console.error('test-mmt-cbor: mmtCbor.js removed (first-party info stream). See docs/INFO_STREAM.md');
process.exit(0);
