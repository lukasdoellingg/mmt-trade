# API direkt zum Backend (Port 3001) – für Charts etc.
VITE_API_URL=http://localhost:3001/api

# Script indicators, bar stats, FeedHub /ws/session (requires web/backend on 3001)
VITE_USE_SESSION_MUX=1
# OB heatmap: 0 = legacy /ws/heatmap + obHeatmapWorker; 1 = chartEngineWorker pipeline
VITE_USE_EMSCRIPTEN_WORKERS=0
# OB heatmap inside chartEngineWorker (opt-in, default off)
# VITE_USE_EMSCRIPTEN_OB_HEATMAP=1
