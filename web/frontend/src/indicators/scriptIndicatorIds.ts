/** Re-export script ids — source of truth is indicatorCatalog.ts (MMT-style). */
export {
  SCRIPT_INDICATOR_IDS,
  SCRIPT_INDICATOR_LABELS,
  SCRIPT_INDICATOR_COLORS,
  type ScriptIndicatorId,
} from './indicatorCatalog';
