# TUI – Terminal-UI (premium, kompakt)
textual>=0.47.0

# API – Börsendaten (CCXT), Rate-Limits eingebaut
ccxt>=4.2.0

# API – async (MMT/weitere)
httpx>=0.27.0

# Chart – Kerzen (X=Zeit, Y=Preis), Volume, MA
pandas>=2.0.0
mplfinance>=0.12.10
matplotlib>=3.7.0

# Optional: grafisches GUI
# customtkinter>=5.2.0
